<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="NLua" />
        <PackageReference Include="Shoko.Abstractions" ExcludeAssets="runtime" />
    </ItemGroup>

    <ItemGroup>
        <!-- Tests build GetName instances from a minimal stand-in via GetName.Wrap. -->
        <InternalsVisibleTo Include="LuaRenamer.Tests" />
    </ItemGroup>

</Project>
